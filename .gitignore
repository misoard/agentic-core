# --- not part of the published package ---
# (how_to_start/ is a toy example — tracked in git but excluded from the wheel,
#  which ships only src/agentic_core. No ignore rule needed for it.)
# personal notes / scratch specs
claude files/
# local Claude Code settings & memory
.claude/

# --- secrets ---
.env
.env.*
!.env.example

# --- build / packaging artifacts ---
dist/
build/
*.egg-info/
.venv/

# --- python caches ---
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- os / editor ---
.DS_Store
